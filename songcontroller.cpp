#include "songcontroller.h"
#include "song.h"
/*SongController:: SongController(Repository repository){
    repo=repository;
}
void SongController::addNewSong(const QString& title, const QString& artist, const QString& link, const QString& lyrics) {
    Song song(title, artist, lyrics, link);
    repo.store(song);
}
void SongController::search(const Song& s){
    repo.find(s.getTitle().toStdString(),s.getArtist().toStdString());
}*/

